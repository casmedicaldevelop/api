// Escenario observable — CreateDeliveryReportDto.
describe('CreateDeliveryReportDto', () => {
  it.todo('acepta deliveryId (IDEntrega) además de miPresEntregaId y valorEntregado')
  it.todo('valorEntregado solo dígitos (regex)')
})
